{
  "name": "ai-tool-init",
  "version": "0.1.0",
  "description": "Repo-local Codex and Claude Code initialization manager.",
  "license": "UNLICENSED",
  "private": true,
  "bin": {
    "ai-tool-init": "src/cli.js"
  },
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=22"
  }
}
