# Team-mounted Skills

This directory is the repo-local team layer.

- Team provider skills from scanned external packages are mounted here.
- The current default provider is `ECC`.
- Mounted skills should resolve from external package roots, not from user-home directories.
- Team guidance should prefer `Bun + TypeScript strict` unless a target project explicitly overrides it.
