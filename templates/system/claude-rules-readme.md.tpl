# Repo-local Claude Rules

This directory is intentionally repo-local.

- Put repository-specific Claude rules here.
- Keep `AGENTS.md` as the primary collaboration contract.
- Do not depend on `~/.claude` for repository truth.
- Prefer `Bun + TypeScript strict` when documenting repo-local engineering defaults.
